[project]
name = "jev-8-ball"
version = "0.1.0"
description = "A single-screen pixel magic 8 ball powered by TypeSafe Jev"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "modal>=1.5.5",
    "uvicorn>=0.53.0",
]
